# 开发计划：退货单（M02）与售后件（M03）全栈实现

## 背景

当前系统所有接口均由 `MockDataProvider` 返回硬编码数据，无真实持久化。Oracle DB 基础设施（JDBC、JPA、Flyway）已配置但 Flyway disabled、APMS_RETURN_ORDER / APMS_PART 表未建。

本计划覆盖三个层面：
1. **状态体系统一** — 解决设计文档/报表需求（中文格式）与代码（英文枚举）的冲突
2. **数据库 + 后端** — Flyway DDL → JPA Entity/Repository/Service → Controller 替换 Mock
3. **前端功能补全** — M02/M03 未实现的功能点

---

## 关键决策：状态字符串策略

**冲突**：设计文档 §3.4 和 `报表需求.md` §4.1 的 SQL 条件均使用中文 `阶段/子状态` 格式（如 `精分析/进行中`）；用户明确要求后端英文编码。

**决策**：DB 存**英文编码**，前端 i18n 转换显示。受影响的文档（设计文档 §3.4、报表需求.md §4.1 SQL 条件）需同步更新为英文编码。

### 状态枚举（统一后）

#### 退货单 — 7 个状态

| 英文编码（DB/代码） | 中文显示 | 颜色 | 触发 |
|---|---|---|---|
| `draft` | 草稿 | default | 新建暂存，无单号 |
| `in_initial_analysis` | 初分析中 | processing | 提交，生成退货单号 |
| `in_detailed_analysis` | 精分析中 | processing | 抽样确认 / 审批驳回 |
| `pending_approval` | 待审批 | warning | 提交精分析报告 |
| `analysis_completed` | 精分析完成 | success | 审批通过 |
| `scrap_in_progress` | WorkOn报废中 | warning | 提交报废申请 |
| `scrapped` | 已报废 | default | 确认 WorkOn 完成（终态）|

#### 售后件 — 6 个状态

| 英文编码（DB/代码） | 中文显示 | 颜色 | 触发 |
|---|---|---|---|
| `in_initial_analysis` | 初分析中 | processing | 新建（PART_NUMBER IS NULL = 草稿）|
| `in_detailed_analysis` | 精分析中 | processing | 被抽样选中 / 审批驳回 |
| `pending_approval` | 待审批 | warning | 提交精分析报告审批 |
| `analysis_completed` | 精分析完成 | success | 审批通过 |
| `scrap_in_progress` | WorkOn报废中 | warning | 报废申请提交 |
| `scrapped` | 已报废 | default | 确认 WorkOn（终态）|

---

## Phase 1：状态体系重构（前端 + Mock）

### 涉及文件
- `frontend/src/types/index.ts`
- `frontend/src/i18n/locales/zh-CN.ts` / `en-US.ts`
- `frontend/src/views/return-orders/OrderList.vue`（statusI18nKeyMap）
- `frontend/src/views/return-orders/OrderDetail.vue`（步骤条 7 步）
- `frontend/src/views/return-parts/PartList.vue`（statusKeyMap）
- `frontend/src/views/return-parts/PartDetail.vue`（statusStepMap、QC_VISIBLE_STATUSES）
- `backend/.../mock/MockDataProvider.java`（status 字段同步）

---

## Phase 2：数据库建表（Flyway）

### 先决条件
`application-local.yml` 启用 Flyway：
```yaml
spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
```

### V1__create_return_order.sql
```sql
CREATE TABLE APMS_RETURN_ORDER (
  ID               VARCHAR2(36 CHAR)  PRIMARY KEY,
  ORDER_NUMBER     VARCHAR2(20 CHAR)  UNIQUE,
  CUSTOMER         VARCHAR2(100 CHAR) NOT NULL,
  RECEIVE_DATE     DATE               NOT NULL,
  COMPLAINT_DATE   DATE               NOT NULL,
  RETURN_METHOD    VARCHAR2(20 CHAR)  NOT NULL,
  TRACKING_NUMBER  VARCHAR2(50 CHAR),
  RETURN_QUANTITY  NUMBER(10)         DEFAULT 0 NOT NULL,
  DESCRIPTION      VARCHAR2(500 CHAR),
  STATUS           VARCHAR2(50 CHAR)  NOT NULL,
  CREATED_BY       VARCHAR2(100 CHAR) NOT NULL,
  CREATED_AT       TIMESTAMP          NOT NULL,
  UPDATED_BY       VARCHAR2(100 CHAR),
  UPDATED_AT       TIMESTAMP
);
```

### V2__create_part.sql
```sql
CREATE TABLE APMS_PART (
  ID                      VARCHAR2(36 CHAR)  PRIMARY KEY,
  PART_NUMBER             VARCHAR2(30 CHAR)  UNIQUE,
  ORDER_ID                VARCHAR2(36 CHAR)  NOT NULL REFERENCES APMS_RETURN_ORDER(ID),
  PART_CODE               VARCHAR2(50 CHAR)  NOT NULL,
  BUSINESS_UNIT           VARCHAR2(50 CHAR)  NOT NULL,
  PRODUCT_PLATFORM        VARCHAR2(50 CHAR)  NOT NULL,
  PRODUCTION_SHIFT        VARCHAR2(20 CHAR),
  COMPLAINT_TYPE          VARCHAR2(20 CHAR),
  VEHICLE_PRODUCTION_DATE DATE,
  VEHICLE_PURCHASE_DATE   DATE,
  VEHICLE_FAILURE_DATE    DATE,
  VEHICLE_VIN             VARCHAR2(17 CHAR),
  VEHICLE_MILEAGE         NUMBER(10),
  CUSTOMER_DESCRIPTION    VARCHAR2(500 CHAR),
  OTHER_DESCRIPTION       VARCHAR2(500 CHAR),
  REPAIR_STATION          VARCHAR2(100 CHAR),
  COMPLAINT_LOCATION      VARCHAR2(100 CHAR),
  RESPONSIBLE_ENGINEER    VARCHAR2(100 CHAR),
  ANALYST                 VARCHAR2(100 CHAR),
  QC_NO                   VARCHAR2(50 CHAR),
  IS_SAMPLE               NUMBER(1)          DEFAULT 0,
  STATUS_CHANGED_AT       TIMESTAMP,
  STATUS                  VARCHAR2(50 CHAR)  NOT NULL,
  CREATED_BY              VARCHAR2(100 CHAR) NOT NULL,
  CREATED_AT              TIMESTAMP          NOT NULL,
  UPDATED_BY              VARCHAR2(100 CHAR),
  UPDATED_AT              TIMESTAMP
);
```

---

## Phase 3：后端持久化层

### 新建文件
- `entity/ReturnOrder.java` — `@Entity @Table(name = "APMS_RETURN_ORDER")`
- `entity/Part.java` — `@Entity @Table(name = "APMS_PART")`（区别于现有 AftermarketPart）
- `repository/ReturnOrderRepository.java` — JpaRepository + JpaSpecificationExecutor
- `repository/PartRepository.java` — JpaRepository + JpaSpecificationExecutor
- `service/ReturnOrderService.java`
- `service/PartService.java`

### ReturnOrderService 关键方法

| 方法 | 业务逻辑 |
|---|---|
| `list(params)` | JPA Specification，按 orderNumber（LIKE）/ customer / status 过滤 |
| `create(dto)` | ID=UUID，STATUS=`draft`，无 ORDER_NUMBER |
| `submit(id)` | draft → in_initial_analysis，生成 ORDER_NUMBER（`{year}EM{seq:04d}`）|
| `sampling(id, req)` | 更新关联 Part IS_SAMPLE，order → in_detailed_analysis |
| `scrap(id, req)` | order → scrap_in_progress |
| `workonConfirm(id)` | order → scrapped |
| `export(params)` | Apache POI 生成 .xlsx |
| `importExcel(file)` | Apache POI 解析，批量 create |

### PartService 关键方法

| 方法 | 业务逻辑 |
|---|---|
| `list(params)` | 过滤：orderNumber(LIKE) / partCode / businessUnit / productPlatform / status / qcCreated |
| `create(dto)` | ID=UUID，STATUS=`in_initial_analysis`，无 PART_NUMBER |
| `submit(id)` | 生成 PART_NUMBER（`{BU}-{PLT}-{seq:04d}`）|
| `updateQcNo(id, qcNo)` | 校验 STATUS ∈ {analysis_completed, scrap_in_progress, scrapped} |

### Controller 新增端点

**ReturnOrderController**：
```
POST   /api/v1/return-orders/{id}/submit
POST   /api/v1/return-orders/{id}/sampling
POST   /api/v1/return-orders/{id}/scrap
POST   /api/v1/return-orders/{id}/scrap/workon-confirm
GET    /api/v1/return-orders/import/template
POST   /api/v1/return-orders/import
GET    /api/v1/return-orders/export
```

**PartController**：
```
POST   /api/v1/parts/{id}/submit
POST   /api/v1/parts/{id}/images
DELETE /api/v1/parts/{id}/images/{imageId}
GET    /api/v1/parts/import/template
POST   /api/v1/parts/import
GET    /api/v1/parts/export
```

### pom.xml 新增
```xml
<dependency>
  <groupId>org.apache.poi</groupId>
  <artifactId>poi-ooxml</artifactId>
  <version>5.2.5</version>
</dependency>
```

---

## Phase 4：前端功能补全

### M02 退货单

| 组件 | 待补全 |
|---|---|
| `OrderList.vue` | 导出 blob 下载；导入 multipart |
| `OrderForm.vue` | 提交后调 POST /{id}/submit，显示 ORDER_NUMBER |
| `OrderDetail.vue` | 7步步骤条；workon-confirm 调用 |
| `SamplingModal.vue` | 携带 sampledPartIds 到 POST /{id}/sampling |
| `ScrapModal.vue` | 调 POST /{id}/scrap |

### M03 售后件

| 组件 | 待补全 |
|---|---|
| `PartList.vue` | 导出 / 导入接口对接 |
| `PartForm.vue` | 提交调 POST /{id}/submit；OCR 调 POST /ocr/recognize；partCode 自动填充 BU/平台 |
| `AnalysisForm.vue` | 暂存/提交审批；模板动态字段渲染；下载报告 |

---

## Phase 5：前后端联调

- 服务端分页：后端返回 `Page<T>`，前端 `pagination.total` 改为 API 值
- 错误处理：`request.ts` 补充 400 处理，表单页补 `message.error()`
- 关键路径验证：退货单全流程 + 售后件全流程 + 导入/导出

---

## 验证方法

1. M02/M03 列表状态标签：正确颜色 + 中文显示
2. 退货单全流程：新建 → 提交生成编号 → 抽样 → 精分析 → 报废 → WorkOn 确认
3. 售后件全流程：新建 → 提交生成件号 → 被抽样选中 → QC No. 录入
4. 导入/导出：模板下载 → 填写 → 导入；列表数据可导出
5. 服务端分页：第 2 页数据来自 API
6. 数据库：Oracle 中 APMS_RETURN_ORDER / APMS_PART 有真实数据
