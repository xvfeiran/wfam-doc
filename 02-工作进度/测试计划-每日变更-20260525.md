# 测试计划：每日变更 2026-05-25

> 创建时间：2026-05-25  
> 关联测试文档版本：v3.17  
> 作者：开发团队

---

## 一、今日变更概览

| # | 功能 | 类型 | 影响范围 | 测试文档节 |
|---|------|------|---------|-----------|
| F1 | 零件生产日期字段 | 新字段 | Part 实体、PartDTO、前端表单/详情、DB V43 | 3.10 |
| F2 | 0km 售后件初始状态 | 业务逻辑 | ComplaintTypeConstants、PartService、AnalysisOrderService、NotificationService | 3.11 |
| F3 | SMB 配置 UI 化 | 新功能 | SmbConfiguration 全栈（Entity/Repo/Service/Controller）、前端设置页、DB V44 | 7.8 |
| F4 | QMC Leader 邮箱动态获取 | 重构 | UserService、UserEmailService、NotificationService、NotificationProperties、yml | 7.7.3 |

---

## 二、本地调试前置配置

### F3 SMB（需要真实 SMB 环境或可 Mock）

```yaml
# application-local.yml 中已有真实 SMB 配置时直接测试；
# 无环境时可用测试连接接口验证报错路径
```

### F4 QMC Leader（三种模式选一）

```yaml
# 模式A - 生产路径（需 AEP 可达）
aep.proxy.enabled: true

# 模式B - 本地 Mock
aep.proxy.enabled: false
custom.notification.mock-qmc-leader-enabled: true
custom.notification.mock-qmc-leader-emails: "your@email.com"

# 模式C - 验证默认告警
aep.proxy.enabled: false
custom.notification.mock-qmc-leader-enabled: false
```

---

## 三、F1：零件生产日期字段

**改动文件：** `Part.java` · `PartDTO.java` · `BasicInfoCard.vue` · `PartForm.vue` · `PartFormModal.vue` · `PartDetail.vue` · `V43__add_part_production_date.sql`

| ID | 测试项 | 步骤 | 预期结果 | 优先级 |
|----|-------|------|---------|-------|
| F1-01 | 新建时可选填生产日期 | 打开新建表单 → 填写「零件生产日期」→ 保存 | 保存成功，详情页显示所填日期 | 🔴 |
| F1-02 | 新建时不填生产日期 | 打开新建表单 → 不填该字段 → 保存 | 保存成功，详情页显示 `-` | 🔴 |
| F1-03 | 编辑时修改生产日期 | 编辑已有售后件 → 修改「零件生产日期」→ 保存 | 保存成功，详情页显示新日期 | 🟡 |
| F1-04 | 编辑时清空生产日期 | 编辑已有售后件 → 清空「零件生产日期」→ 保存 | 保存成功，详情页显示 `-` | 🟡 |
| F1-05 | 字段位置正确 | 打开新建/编辑表单 | 「零件生产日期」位于「生产班次」字段**之前** | 🟢 |
| F1-06 | 导入不影响 | 用标准 Excel 导入售后件 → 查看详情 | 「零件生产日期」为空（`-`），无导入报错 | 🟡 |
| F1-07 | DB 迁移 V43 执行正常 | 检查 `APMS_PART` 表结构 | 存在 `PART_PRODUCTION_DATE DATE NULL` 列 | 🟡 |

---

## 四、F2：0km 售后件初始状态

**改动文件：** `ComplaintTypeConstants.java`（新建）· `ComplaintTypeConstantsTest.java`（新建）· `PartService.java` · `AnalysisOrderService.java` · `NotificationService.java`

### 4.1 ComplaintTypeConstants 单元测试

| ID | 测试项 | 步骤 | 预期结果 | 优先级 |
|----|-------|------|---------|-------|
| F2-01 | 单元测试全部通过 | 运行 `mvn test -Dtest=ComplaintTypeConstantsTest` | 7 条用例全通过（BA40/BA41 非 0km，BA20/BA35/BA42 是 0km，null/空 非 0km） | 🔴 |

### 4.2 售后件初始状态路由

| ID | 测试项 | 步骤 | 预期结果 | 优先级 |
|----|-------|------|---------|-------|
| F2-02 | 0km 退货单新建售后件 | 创建 `complaintType=BA20` 退货单 → 新建售后件 | 售后件初始状态为 `analysis_skipped` | 🔴 |
| F2-03 | 非 0km 退货单新建售后件 | 创建 `complaintType=BA40` 退货单 → 新建售后件 | 售后件初始状态为 `in_initial_analysis` | 🔴 |
| F2-04 | BA41 非 0km | 创建 `complaintType=BA41` 退货单 → 新建售后件 | 售后件初始状态为 `in_initial_analysis` | 🟡 |
| F2-05 | complaintType 为空视为非 0km | 创建 `complaintType` 为空的退货单 → 新建售后件 | 售后件初始状态为 `in_initial_analysis` | 🟡 |

### 4.3 后续流程验证

| ID | 测试项 | 步骤 | 预期结果 | 优先级 |
|----|-------|------|---------|-------|
| F2-06 | 0km 件可触发报废 | 找到 `analysis_skipped` 状态售后件 → 进入分析单 → 执行报废 | 报废成功，无权限或状态错误（isSample 默认为 0） | 🔴 |
| F2-07 | 非 0km 件不触发 0km 通知 | 新建 `complaintType=BA40` 售后件 → 检查通知日志 | 不发送 0km 通知邮件 | 🟡 |
| F2-08 | BA35 触发 0km 通知（新增口径）| 新建 `complaintType=BA35` 售后件 → 检查通知日志 | 发送 0km 通知邮件（旧逻辑可能遗漏此类型） | 🟡 |

---

## 五、F3：SMB 配置 UI 化

**改动文件：** `SmbConfiguration.java`（新）· `SmbConfigurationRepository.java`（新）· `SmbConfigurationDTO.java`（新）· `SmbConfigurationService.java`（新）· `SmbFileStorageService.java`（改）· `LocalFileStorageService.java`（改）· `SmbConfigurationController.java`（新）· `SmbjConfig.java`（改）· `SmbConfig.vue`（新）· `Settings.vue`（改）· `smbConfigApi.ts`（新）· `zh-CN.ts` / `en-US.ts`（改）· `V44__create_smb_configuration.sql`（新）

### 5.1 无配置时的防护

| ID | 测试项 | 步骤 | 预期结果 | 优先级 |
|----|-------|------|---------|-------|
| F3-01 | 无配置时文件上传返回 503 | DB 无 SMB 记录 → 上传文件 | 返回 503，提示"SMB 未配置或已禁用" | 🔴 |
| F3-02 | 无配置时文件下载返回 503 | DB 无 SMB 记录 → 下载文件 | 返回 503，提示"SMB 未配置或已禁用" | 🔴 |
| F3-03 | enabled=false 时操作返回 503 | 保存 `enabled=false` → 上传文件 | 返回 503 | 🔴 |

### 5.2 配置加载与显示

| ID | 测试项 | 步骤 | 预期结果 | 优先级 |
|----|-------|------|---------|-------|
| F3-04 | 首次无配置进入页面 | DB 无记录 → 进入设置-SMB存储配置 | 表单为空/默认值，无报错 | 🟡 |
| F3-05 | 已有配置时加载 | DB 有记录 → 进入页面 | 表单展示已保存值，密码字段显示 `******` | 🔴 |
| F3-06 | GET 接口密码脱敏 | `GET /api/v1/smb-configuration` | 响应中 password 为 `"******"` | 🔴 |

### 5.3 保存与热重载

| ID | 测试项 | 步骤 | 预期结果 | 优先级 |
|----|-------|------|---------|-------|
| F3-07 | 保存正确配置后立即可用 | 填写正确 SMB 信息 → 保存 → 立即上传文件 | 无需重启，文件操作成功（热重载验证） | 🔴 |
| F3-08 | 不改密码时 DB 原密码保留 | 加载配置（密码=`******`）→ 修改其他字段 → 保存 | DB 密码不变，文件操作仍可用 | 🔴 |
| F3-09 | 改为错误密码后操作失败 | 将密码改为错误值 → 保存 → 上传文件 | 文件操作失败，无 500 崩溃 | 🔴 |
| F3-10 | 必填项校验 | 清空 host/shareName/user/password/prefix/env → 点击保存 | 各字段均有校验提示，不提交 | 🟡 |

### 5.4 测试连接

| ID | 测试项 | 步骤 | 预期结果 | 优先级 |
|----|-------|------|---------|-------|
| F3-11 | 测试连接成功 | 正确配置 → 保存 → 点击测试连接 | 显示成功提示 | 🔴 |
| F3-12 | 测试连接失败 | 错误 host → 保存 → 点击测试连接 | 显示失败提示，含错误原因 | 🔴 |
| F3-13 | 未保存时禁用测试连接 | 修改配置但不保存 | 测试连接按钮禁用 | 🟡 |

### 5.5 DB 迁移

| ID | 测试项 | 步骤 | 预期结果 | 优先级 |
|----|-------|------|---------|-------|
| F3-14 | V44 迁移执行成功 | 检查 `APMS_SMB_CONFIGURATION` 表结构 | 表存在，字段与设计文档一致 | 🟡 |

---

## 六、F4：QMC Leader 邮箱动态获取

**改动文件：** `UserService.java` · `UserEmailService.java` · `NotificationService.java` · `NotificationProperties.java` · `application-local.yml` · `application-test.yml`

### 6.1 配置加载

| ID | 测试项 | 步骤 | 预期结果 | 优先级 |
|----|-------|------|---------|-------|
| F4-01 | 新配置键正常加载 | yml 配置 `mock-qmc-leader-enabled: false` → 启动 | 启动成功，无 ConfigurationProperties 异常 | 🟢 |
| F4-02 | 旧键名移除 | yml 中写入旧键 `qmc-leader-emails: x` | 启动成功，旧键被忽略 | 🟢 |

### 6.2 路径 A：AEP 启用

| ID | 测试项 | 步骤 | 预期结果 | 优先级 |
|----|-------|------|---------|-------|
| F4-03 | AEP 正常返回 QMC Leader 邮箱 | AEP 可达 → 触发含 QMC Leader 的通知 | QMC_Leader + QMC_Manager 两角色成员均收到邮件 | 🔴 |
| F4-04 | AEP 异常不崩溃 | 模拟 AEP 500 → 触发通知 | 日志 ERROR；主收件人（ANALYST）正常收到；无异常抛出 | 🔴 |
| F4-05 | 两角色重叠用户去重 | AEP 某用户同属两角色 | 该邮箱只出现一次 | 🟡 |

### 6.3 路径 B：Mock 调试

| ID | 测试项 | 步骤 | 预期结果 | 优先级 |
|----|-------|------|---------|-------|
| F4-06 | Mock 邮箱正常生效 | `mock-qmc-leader-enabled=true` + `mock-qmc-leader-emails=a@b.com` → 触发通知 | a@b.com 收到邮件 | 🔴 |
| F4-07 | Mock 邮箱为空时 warn | `mock-qmc-leader-enabled=true` + emails 为空 | 空列表 + WARN 日志；其他收件人正常收到 | 🟡 |

### 6.4 路径 C：默认行为

| ID | 测试项 | 步骤 | 预期结果 | 优先级 |
|----|-------|------|---------|-------|
| F4-08 | 默认返回空列表并告警 | `aep.enabled=false`，`mock-qmc-leader-enabled=false` → 触发通知 | WARN 日志；无 QMC Leader 邮件；其他收件人正常 | 🔴 |

### 6.5 通知流程回归

| ID | 测试项 | 步骤 | 预期结果 | 优先级 |
|----|-------|------|---------|-------|
| F4-09 | 责任判定通知 QMC Leader 抄送 | 触发 RESPONSIBILITY=B/O → 检查收件箱 | QMC Leader 收到 CC 邮件 | 🔴 |
| F4-10 | 超时通知 QMC Leader 抄送 | 售后件超期 → 定时通知触发 | QMC Leader 收到 CC 邮件 | 🔴 |
| F4-11 | 审批超期提醒 QMC Leader 为主收件人 | 审批超期 → 定时通知触发 | QMC Leader 收到 TO 邮件 | 🔴 |

---

## 七、测试完成进度

| 功能 | 总计 | 已通过 | 待测试 |
|------|------|-------|-------|
| F1 零件生产日期 | 7 | 0 | 7 |
| F2 0km 初始状态 | 8 | 0 | 8 |
| F3 SMB 配置 UI 化 | 14 | 0 | 14 |
| F4 QMC Leader 邮箱 | 11 | 0 | 11 |
| **合计** | **40** | **0** | **40** |

---

## 八、优先级汇总

| 优先级 | 数量 | 用例 |
|-------|------|-----|
| 🔴 高（核心流程） | 20 | F1-01/02、F2-01/02/03/06、F3-01/02/03/05/06/07/08/09/11/12、F4-03/04/06/08/09/10/11 |
| 🟡 中（边界/回归）| 14 | F1-03/04/06/07、F2-04/05/07/08、F3-04/10/13/14、F4-05/07 |
| 🟢 低（配置默认）| 3 | F1-05、F4-01/02 |
