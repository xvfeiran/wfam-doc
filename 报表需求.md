# 统计报表模块需求说明（M06）

## 1. 通用规则

### 1.1 页面结构

报表页顶部设**全局筛选栏**，下方为各图表区域，最底部为**列表明细**（始终显示售后件数据）。

### 1.2 全局筛选条件

以下筛选条件统一放置在页面顶部，作用于页面上**所有图表和列表**：

| 筛选项 | 类型 | 说明 |
|------|------|------|
| 日期范围 | 日期区间选择器 | 筛选退货单收件日期或售后件创建时间，默认当年 1 月 1 日至今 |
| 客户 | 下拉单选 | 对应 APMS_CUSTOMER |
| BU | 下拉单选 | 对应 APMS_BUSINESS_UNIT |
| 产品平台 | 下拉单选 | 对应 APMS_PRODUCT_PLATFORM（联动 BU） |
| Mis 时间段 | 下拉单选 | 见 1.5 节，仅对 field 类（非 0km）售后件有效 |

> 各图表可在全局筛选基础上叠加自身额外筛选条件（在图表区内配置）。

### 1.3 同比对比（YoY）

- **全局开关**：打开后，所有支持同比的图表同时切换到同比模式。
- **图表单独开关**：每个支持同比的图表也有独立开关，可覆盖全局设置。
- **同比逻辑**：当前年 YTD 数据 vs. 上一年同期数据，并排或叠加展示。

### 1.4 YTD（Year-to-Date）

带有"YTD"标注的图表默认展示**当年 1 月 1 日至今**的数据。YoY 开启时，同时展示上一年同期（1 月 1 日至同月同日）的数据。

### 1.5 Mis 时间段定义

**Mis（Months in Service）**= 车辆在役月数，由系统根据已有字段自动计算：

```
Mis = FLOOR((VEHICLE_FAILURE_DATE - VEHICLE_PURCHASE_DATE) / 30)
```

筛选时可选择具体 Mis 时间段（Mis1、Mis2、Mis3 等），各时间段的月份区间**待业务确认后补充**。

> **注意**：仅 field 类投诉（非 0km，即非 BA10/BA20/BA21/BA30/BA31）的售后件拥有车辆日期数据，0km 件不参与 Mis 计算，Mis 筛选对 0km 件无效。

### 1.6 多维度动态柱状图交互规则

第 2.2、2.3、2.4 节的柱状图均支持动态维度配置：

- **主维度**（X 轴）：用户在图表区选择，X 轴标签由此决定。
- **副维度**（堆叠颜色）：可选，用于将每根柱子按第二个维度拆分为颜色区块。
- **Y 轴**：固定为计数或均值（依图表类型而定）。

可选维度因图表而异，详见各图表说明。

### 1.7 导出

每个图表提供**单独导出**按钮，导出当前图表数据为 Excel（.xlsx），文件名格式：`{图表名称}_{yyyyMMdd}.xlsx`。

---

## 2. 图表需求

### 2.1 退货单

#### 2.1.1 KPI 卡片

| 项目 | 说明 |
|------|------|
| 指标 | 退货单总数（按当前全局筛选条件过滤） |
| 展示方式 | 数字卡片 |
| 无同比 | — |

#### 2.1.2 客户分布玫瑰图

| 项目 | 说明 |
|------|------|
| 图表类型 | 玫瑰图（ECharts Nightingale） |
| X / 扇区 | 客户名称 |
| 值 | 退货单数量 |
| 筛选条件 | 日期范围（可在图表内单独设置） |
| 默认状态 | 展示所有时间的退货单按客户分布 |
| 同比 | 不支持 |

#### 2.1.3 退货趋势折线图

| 项目 | 说明 |
|------|------|
| 图表类型 | 折线图 |
| X 轴 | 时间周期（可选：周 / 月 / 季 / 年，默认月） |
| Y 轴 | 退货单数量 |
| 筛选条件 | 日期范围 + 可选客户筛选 |
| 默认状态 | 当年按月展示所有退货单趋势 |
| 同比 | 支持（上一年同期折线叠加显示） |

---

### 2.2 售后件

#### 2.2.1 KPI 卡片

| 项目 | 说明 |
|------|------|
| 指标 | 售后件总数（按当前全局筛选条件过滤） |
| 展示方式 | 数字卡片 |

#### 2.2.2 多维度柱状图（YTD）

| 项目 | 说明 |
|------|------|
| 图表类型 | 堆叠柱状图（YTD） |
| Y 轴 | 售后件数量 |
| 同比 | 支持 |

**可选主维度（X 轴）：**

| 维度值 | X 轴标签示例 |
|------|------|
| 周期（默认月） | 1 月、2 月、3 月… |
| 客户 | 宝马、大众、特斯拉… |
| BU | BU1、BU2… |
| 产品平台 | WSH、WSM、PCE… |
| Mis 时间段 | Mis1、Mis2、Mis3… |

**可选副维度（堆叠颜色）：**同上，可与主维度任意组合（主副维度不同即可）。

**示例：**
- 筛选：宝马；主维度：周期（月）；副维度：BU → X 轴为各月份，每柱按 BU 颜色堆叠
- 筛选：TS（产品类型）；主维度：客户 → X 轴为各客户，单色柱
- 筛选：Mis3；主维度：产品平台 → X 轴为各产品平台，单色柱

---

### 2.3 抽样件（精分析件）

> **数据范围**：抽样件 = 售后件中状态为 `精分析/进行中` 及之后所有状态的记录。

#### 2.3.1 KPI 卡片

| 项目 | 说明 |
|------|------|
| 指标 | 抽样件总数（按当前全局筛选条件过滤） |
| 展示方式 | 数字卡片 |

#### 2.3.2 多维度柱状图（YTD）

| 项目 | 说明 |
|------|------|
| 图表类型 | 堆叠柱状图（YTD） |
| Y 轴 | 抽样件数量 |
| 同比 | 支持 |

**可选主维度（X 轴）：**

| 维度值 | X 轴标签示例 | 数据来源 |
|------|------|------|
| 周期（默认月） | 1 月、2 月… | 系统时间 |
| 客户 | 宝马、大众… | APMS_CUSTOMER |
| BU | BU1、BU2… | APMS_BUSINESS_UNIT |
| 产品平台 | WSH、WSM… | APMS_PRODUCT_PLATFORM |
| Mis 时间段 | Mis1、Mis2… | 计算字段 |
| 失效模式 | 噪音、断裂、外观… | APMS_FAILURE_CATEGORY |
| 责任判定 | 我方责任、客户责任… | 精分析报告字段（见 3.3 节） |

**可选副维度（堆叠颜色）：**同上。

**额外筛选条件（图表内）：**

| 筛选项 | 说明 |
|------|------|
| 失效模式 | 按失效类别过滤 |
| 责任判定 | 按责任判定值过滤（暂定：我方责任 / 客户责任 / 共同责任 / 无法判定，具体待确认） |

**示例：**
- 筛选：宝马；主维度：月；副维度：失效模式 → 各月按失效模式堆叠
- 筛选：TS；主维度：失效模式 → X 轴为各失效模式
- 筛选：产品平台=X；主维度：失效模式；副维度：责任判定 → 各失效模式柱子按责任判定颜色堆叠

---

### 2.4 分析时长

#### 2.4.1 柱状图（YTD）

| 项目 | 说明 |
|------|------|
| 图表类型 | 柱状图（YTD） |
| Y 轴 | 平均分析时长（单位待定，建议天） |
| 同比 | 支持 |
| 计算起止点 | **待确认**（候选：精分析开始 → 报告审批通过） |

**可选主维度（X 轴）：**

| 维度值 | 说明 |
|------|------|
| BU（默认） | 各 BU 平均分析时长 |
| 客户 | 各客户平均分析时长 |
| 产品平台 | 各产品平台平均分析时长 |
| 失效模式 | 各失效类别平均分析时长 |
| 责任人 | 各分析员平均分析时长 |
| Mis 时间段 | 各 Mis 区间平均分析时长 |

**筛选条件：** Mis 时间段 + 客户 + BU + 产品平台 + 失效模式

---

### 2.5 售后件状态

#### 2.5.1 状态分布柱状图

| 项目 | 说明 |
|------|------|
| 图表类型 | 柱状图 |
| X 轴 | 售后件状态（固定维度，不可配置）：`初分析/进行中` / `精分析/进行中` / `精分析审批/进行中` / `精分析审批/完成` / `WorkOn报废/进行中` / `WorkOn报废/完成` |
| Y 轴 | 售后件数量 |
| 筛选条件 | Mis 时间段 + 客户 + BU + 产品平台 |
| 同比 | 不支持（状态分布为当前快照，非时序数据） |

---

## 3. 列表明细

| 项目 | 说明 |
|------|------|
| 数据源 | 始终为**售后件**，不随图表分类切换 |
| 字段 | 与售后件列表（M03）字段保持一致 |
| 筛选 | 与全局筛选条件联动（不需要额外筛选栏） |
| 分页 | 与 M03 保持一致 |
| 操作按钮 | 至少支持"查看详情"，导航至 M03 售后件详情页 |

---

## 4. 数据源与计算规则

### 4.1 数据范围

| 指标分类 | 数据源表 | 过滤条件 |
|------|------|------|
| 退货单 | APMS_RETURN_ORDER | — |
| 售后件 | APMS_PART | — |
| 抽样件（精分析件） | APMS_PART | STATUS IN (`精分析/进行中`, `精分析审批/进行中`, `精分析审批/完成`, `WorkOn报废/进行中`, `WorkOn报废/完成`) |
| 分析时长 | APMS_PART + APMS_ANALYSIS_REPORT | 同上 |
| 售后件状态 | APMS_PART | — |

### 4.2 Mis 计算逻辑

```sql
-- 在售后件查询中作为派生字段使用
FLOOR((VEHICLE_FAILURE_DATE - VEHICLE_PURCHASE_DATE) / 30) AS mis_months
-- 然后按 mis_months 归入对应 Mis 时间段（区间定义待确认）
```

仅适用于 COMPLAINT_TYPE 非 0km 类型（即排除 BA10/BA20/BA21/BA30/BA31）。

### 4.3 责任判定字段来源

"责任判定"存储于 `APMS_ANALYSIS_REPORT.CONTENT_JSON`，是精分析报告模板中的一个标准字段。

**架构注意**：CONTENT_JSON 为 CLOB（JSON），直接在聚合查询中使用 Oracle `JSON_VALUE()` 提取性能较低。建议在 `APMS_ANALYSIS_REPORT` 表中新增 `RESPONSIBILITY_JUDGMENT VARCHAR2(50)` 冗余列，在报告提交/审批时同步写入，供报表查询使用。

可选值（暂定，具体待业务确认）：`我方责任` / `客户责任` / `共同责任` / `无法判定`

### 4.4 分析时长计算（待确认）

**待确认**：分析时长的计算起止点。候选方案：
- 方案 A：精分析开始（Part 进入 `精分析/进行中`）→ 精分析报告审批通过（AnalysisReport STATUS = `approved`）
- 方案 B：售后件初分析提交 → 精分析报告审批通过

---

## 5. 待确认项

| 编号 | 问题 | 影响范围 |
|------|------|------|
| P1 | Mis 时间段区间划分（Mis1/Mis2/Mis3 各对应几个月？） | 2.2、2.3、2.4、筛选项 |
| P2 | 责任判定可选值的完整枚举 | 2.3、2.4 |
| P3 | 分析时长计算起止点 | 2.4 |
| P4 | 卡片是否需要展示多个数字（如分状态数量），还是仅一个总数 | 2.1.1、2.2.1、2.3.1 |
